# NixOS 仓库重构提示词

你现在在一个 NixOS 仓库里工作。请把现有配置重构为清晰、可维护、分层明确的 declarative system architecture。目标不是改功能，而是重组结构、降低耦合、让后续维护更简单。重构时必须尽量保持现有系统行为不变，优先“移动 / 拆分 / 归类 / 抽取模块”，不要随意改动功能语义。

## 总目标

当前仓库里已经有 `flake.nix`、`hardware-configuration.nix`、`home/`、`modules/`、`hosts/MEOW/`、`secrets/` 等目录，但结构还不够清晰，很多系统级、桌面级、硬件级、用户级配置混在一起。请按下面原则整理：

- 系统入口应形成：`flake.nix -> hosts/MEOW/default.nix -> modules/* -> home/Reiky-REI`
- `hosts/MEOW/default.nix` 只能作为 host composition root，主要负责 imports，不应继续承载大量具体配置
- `hardware-configuration.nix` 只保留机器探测和硬件相关的底层结果，不要把软件包、桌面、服务策略塞进去
- 系统级策略、桌面策略、服务策略、用户态配置必须拆分

## 目录分层建议

建议形成这些层级：

```text
modules/
├── common/
├── hardware/
├── desktop/
├── networking/
├── services/
├── development/
└── packages/
```

### 各层职责

#### `modules/common/`
放所有全局基础设置，例如：

- `nix.settings.*`
- `nixpkgs.config.allowUnfree`
- `nix.gc.*`
- `time.timeZone`
- `i18n.*`
- `console.*`
- `fonts.*`
- `programs.nix-ld.enable`
- 其他全局基础策略

#### `modules/hardware/`
放 CPU / GPU / 蓝牙 / 音频 / 设备相关策略，例如：

- microcode
- NVIDIA / Intel / AMD GPU 配置
- CUDA 相关内容
- `hardware.graphics.*`
- `hardware.bluetooth.enable`
- `services.blueman.enable`
- 其他硬件抽象层配置

#### `modules/desktop/`
放 Wayland / X11 会话栈、display manager、compositor、通知中心、idle/lock、portal、xwayland、桌面相关环境变量等：

- `programs.hyprland`
- `programs.niri`
- `programs.xwayland.enable`
- `services.displayManager.*`
- `services.swaync.*`
- `services.swayidle.*`
- `xdg.portal.*`
- 桌面相关主题和会话层设置

#### `modules/networking/`
放网络、代理、防火墙、SSH、VPN 等基础网络能力：

- `networking.networkManager.enable`
- `networking.proxy`
- `networking.firewall.*`
- `services.openssh.*`
- 其他网络服务

#### `modules/services/`
放后台 daemon、系统能力服务：

- `services.printing.enable`
- `services.udisks2.enable`
- `services.timesyncd.enable`
- `services.flatpak.enable`
- `services.pipewire.*`
- `services.mpd.*`
- `syncthing`
- `avahi`
- 其他长期后台服务

#### `modules/development/`
放系统级开发工具链和平台支持，例如：

- 编译器 / SDK / 工具链
- 机器人相关平台支持
- CUDA 工具链（若作为系统层使用）
- 其他开发环境基础设施

#### `modules/packages/`
只放少量明确属于 system-wide 的基础包，不要把所有软件都堆在这里。

---

## System layer 与 Home layer 的边界

请严格区分：

### system layer（NixOS modules）
负责：

- daemon
- kernel
- hardware
- system capability
- background infrastructure
- 图形会话基础设施

例如：

- `mpd`
- `pipewire`
- `docker`
- `ssh`
- `flatpak`
- `nvidia` driver
- `hyprland`
- `niri`
- `greetd` / `ly` / `sddm`

### home layer（home-manager）
负责：

- 用户应用
- shell
- editor
- WM config
- terminal tools
- GUI apps
- 用户偏好

例如：

- `kitty`
- `rofi`
- `go-musicfox`
- `ncmpcpp`
- `yazi`
- `fastfetch`
- `zsh` 配置
- 个人快捷键和脚本
- 桌面主题的用户态部分

---

## 具体迁移规则

### 1. 迁移到 `modules/common/`
以下内容应迁移到 `modules/common/`：

- `nix.settings.trusted-users`
- `nix.settings.experimental-features`
- `nixpkgs.config.allowUnfree`
- `nix.gc`
- `time.timeZone`
- `i18n.*`
- `console.*`
- `fonts.*`
- `programs.nix-ld.enable`

### 2. 迁移到 `modules/networking/`
以下内容应迁移到 `modules/networking/`：

- `networking.networkmanager.enable`
- `networking.proxy`
- `networking.firewall.*`
- `services.openssh.*`

### 3. 迁移到 `modules/services/`
以下内容应迁移到 `modules/services/`：

- `services.printing.enable`
- `services.udisks2.enable`
- `services.timesyncd.enable`
- `services.flatpak.enable`
- `services.pipewire.*`
- `services.mpd.*`

### 4. 迁移到 `modules/hardware/`
以下内容应迁移到 `modules/hardware/`：

- `hardware.bluetooth.enable`
- `services.blueman.enable`
- `hardware.cpu.*.updateMicrocode`
- `hardware.graphics.*`
- `intel-media-driver`
- `intel-vaapi-driver`
- NVIDIA / CUDA 相关内容

### 5. 迁移到 `modules/desktop/`
以下内容应迁移到 `modules/desktop/`：

- `programs.hyprland.*`
- `programs.niri.*`
- `programs.xwayland.enable`
- `services.displayManager.ly.enable`
- `services.swaync.enable`
- `services.swayidle.enable`
- 桌面相关环境变量
- 桌面主题 / 会话组件 / portal 相关配置

### 6. 保持在 `home/`
以下内容应保持在 `home/Reiky-REI/` 下：

- `go-musicfox`
- `kitty`
- `rofi`
- `waybar`
- `wlogout`
- `shell` 配置
- `terminal` 配置
- 其他用户级应用和交互配置

---

## 环境变量的归类规则

请按语义拆分 `environment.sessionVariables`，不要把所有变量堆在 host 文件里。

### 属于 desktop / Wayland
- `NIXOS_OZONE_WL = "1"`

### 属于 desktop / input method
- `QT_IM_MODULE = "fcitx"`
- `QT5_IM_MODULE = "fcitx"`
- `XMODIFIERS = "@im=fcitx"`

### 属于 home / user preference
- `TERMINAL = "kitty"`

### 属于 services / flatpak
- `XDG_DATA_DIRS` 中的 Flatpak export path

---

## 现有文件的处理方式

### `hosts/MEOW/default.nix`
最终应该尽量只负责 imports，例如：

```nix
{
  imports = [
    ./hardware.nix
    ../../modules/common
    ../../modules/hardware
    ../../modules/desktop
    ../../modules/networking
    ../../modules/services
    ../../home/Reiky-REI
  ];
}
```

不要再让这个文件长篇堆积具体 option。

### `hosts/MEOW/hardware.nix`
只保留从 `hardware-configuration.nix` 迁移来的 host-specific 硬件底层内容。

如果当前存在拼写错误 `hardwaer.nix`，请先修正为 `hardware.nix`。

### `flake.nix`
只做输入输出和系统实例拼装，不要堆业务逻辑。

---

## 建议的目录结构

请尽量整理成类似下面的结构：

```text
/etc/nixos
├── flake.nix
├── flake.lock
├── hosts
│   └── MEOW
│       ├── default.nix
│       └── hardware.nix
├── modules
│   ├── common
│   ├── hardware
│   ├── desktop
│   ├── networking
│   ├── services
│   ├── development
│   └── packages
├── home
│   └── Reiky-REI
└── secrets
```

---

## README.md 要求

请补写仓库根目录的 `README.md`，作为维护指南，而不是流水账。README 至少应说明：

1. 仓库目标
2. 分层原则
3. 目录树说明
4. 每一层放什么、不放什么
5. `host` / `module` / `home-manager` 的职责边界
6. 如何新增一个系统模块
7. 如何新增一个 home module
8. 如何判断软件该归类到 `services`、`desktop` 还是 `home`
9. 如何 rebuild
10. 如何排查配置归属错误

### README 中应明确给出分类规则
例如：

- daemon / 后台长期运行 => `services`
- 图形会话入口 / Wayland stack => `desktop`
- 用户交互应用 => `home`
- 硬件驱动和微码 => `hardware`
- 全局基础设置 => `common`

### README 中应给出常见示例
例如：

- `ssh` -> `networking` / `services`
- `flatpak` -> `services`
- `mpd` -> `services`
- `swaync` -> `desktop`
- `swayidle` -> `desktop`
- `go-musicfox` -> `home`
- `NVIDIA` / `CUDA` -> `hardware`
- `TERMINAL` -> `home`
- `XDG_DATA_DIRS` -> `services/flatpak`
- `QT_IM_MODULE` / `XMODIFIERS` -> `desktop`

---

## 修改原则

请遵守以下原则：

- 不要大改功能
- 不要删掉现有可用能力
- 不要为了美观而引入新的行为变化
- 遇到难以判定的归类，优先保持现有行为不变，再把它放到最接近语义的模块
- 修改后尽量保证 `nixos-rebuild switch --flake /etc/nixos#MEOW` 可通过
- 确保路径引用、imports、module 名称、文件名都一致
- 不要改动 `secrets/` 里的实际密钥内容

---

## 交付标准

请输出：

1. 重构后的目录结构
2. 各文件移动 / 新增 / 修改清单
3. `README.md` 的内容
4. 必要时的简短说明，解释为什么某些项归入 `desktop`、`services`、`hardware` 或 `home`

目标是让仓库从“配置堆积”变成“清晰的系统架构”。
